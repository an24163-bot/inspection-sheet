# Streamlit Cloud 배포 — 따라하기 가이드

이 폴더(`cloud_deploy`)에 들어 있는 모든 파일을 GitHub 에 올린 뒤
streamlit.io 에 연결하면 영구 URL 이 만들어집니다.

## 1단계 — GitHub 계정 + 저장소 만들기 (5분, 한 번만)

### A. GitHub 가입
1. https://github.com 접속
2. **Sign up** → 이메일 (예: `ys@saehannanotech.com`) → 비밀번호 → 사용자명 (예: `saehannano`)
3. 이메일 인증 → 무료 계정 완성

### B. 새 저장소 만들기
1. 우측 상단 **`+`** 클릭 → **New repository**
2. Repository name: `inspection-sheet` (또는 원하는 이름)
3. **Private** 선택 (회사 자료라 비공개 권장)
4. README, .gitignore, license 모두 체크 안 함 (이미 우리가 가지고 있음)
5. **Create repository** 클릭

## 2단계 — 파일 업로드 (3분)

### 가장 간단한 방법: 웹에서 드래그앤드롭

1. 방금 만든 저장소 페이지에서 **uploading an existing file** 링크 클릭
2. 파일 탐색기에서 `cloud_deploy` 폴더 열기
3. **폴더 안의 모든 파일+폴더를 GitHub 페이지로 드래그**
   - app.py, generate_reports.py, vendors.json
   - template_atech.xlsx, template_solmix.xlsx
   - logo_icon.png, stamp.png
   - packages.txt, requirements.txt
   - README.md, .gitignore
   - **`.streamlit` 폴더도 통째로** (안에 config.toml 있음)
4. 아래 **Commit changes** 박스 → **Commit changes** 버튼

### 주의: `.streamlit` 폴더는 점(.)으로 시작
일부 브라우저는 점으로 시작하는 폴더를 숨김 처리하니, 파일 탐색기에서
**보기 → 숨김 항목** 체크 후 드래그.

## 3단계 — Streamlit Cloud 연결 (5분)

1. https://share.streamlit.io 접속
2. **Sign in with GitHub** 클릭 → GitHub 로그인 + 권한 허용
3. 메인 화면에서 **Create app** 또는 **New app** 클릭
4. 다음 정보 입력:
   - **Repository**: 본인계정/inspection-sheet
   - **Branch**: main
   - **Main file path**: `app.py`
   - **App URL** (선택): `inspection-sheet-saehan` 같은 원하는 이름
5. **Deploy** 클릭

## 4단계 — 5~10분 기다리기 (커피 한 잔)

Streamlit Cloud 가 다음을 자동으로 처리합니다:
- 가상 머신 준비
- packages.txt 의 LibreOffice + 한글 폰트 설치 (3~5분)
- requirements.txt 의 Python 패키지 설치 (2~3분)
- app.py 실행

진행 로그가 화면에 흐릅니다. 마지막에 **"Your app is live!"** 메시지가 나오면 완성.

## 5단계 — URL 공유

영구 URL 이 다음 형식으로 만들어집니다:
```
https://inspection-sheet-saehan.streamlit.app
```

이 링크를 직원에게 공유 → 직원은 링크 클릭만으로 사용. 설치 0개.

## 코드 수정 후 자동 배포

본인 PC 에서 코드 수정 → GitHub 에 commit/push → Streamlit Cloud 가
**자동으로 감지해서 1~2분 내 새 버전 배포**. 직원에게 별도 안내 불필요.

GitHub 웹에서 직접 수정도 가능 (간단한 수정용):
- 저장소 페이지 → 수정할 파일 클릭 → 연필 아이콘 → 수정 → Commit

## 보안 / 제한

- **Private 저장소**: 본인만 코드 볼 수 있음 (GitHub 무료 계정도 OK)
- **App Public URL**: 누구나 URL 알면 접속 가능. 인증 추가하려면:
  - Streamlit Cloud 설정에서 **Viewer access** 를 본인 GitHub 계정으로 제한
  - 또는 앱에 비밀번호 입력 추가 (`streamlit-authenticator` 라이브러리)
- **데이터 처리**: 업로드한 ZIP 은 임시 컨테이너에서 처리 후 자동 삭제,
  Streamlit 서버에 영구 저장 안 됨. 그래도 회사 보안정책 확인 권장.
- **사용량**: 무료 계정은 앱 1개 + 비공개 저장소 무제한. 동시 접속자 제한도 충분.

## 자주 막히는 곳

| 증상 | 해결 |
|---|---|
| 배포 실패 "ModuleNotFoundError" | requirements.txt 누락 → 추가 후 commit |
| LibreOffice 못 찾음 | packages.txt 누락 → 추가 후 commit |
| 한글이 □ 표시 | packages.txt 에 fonts-noto-cjk 가 있는지 확인 |
| 앱이 멈춤 | Streamlit Cloud 화면 우측 상단 **Reboot app** 클릭 |
| 변경사항 반영 안 됨 | GitHub 에서 commit 했는지 확인 + Cloud 화면에서 Reboot |

## 프린터 출력 기능에 대해

클라우드 환경에서는 사내 프린터 직접 출력 못 합니다 (사내망 접근 불가).
대신 직원이 PDF 다운로드 받아서 본인 PC 에서 인쇄.
나중에 회사가 VPN 도입하거나 프린터에 Email RX 를 켜면 우회 가능.
